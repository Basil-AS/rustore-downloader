services:
  backend:
    build: ./backend
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:8080"
    env_file:
      - ./backend/.env
    volumes:
      - ./data/gplaydl:/root/.config/gplaydl
      - ./data/merge:/data
